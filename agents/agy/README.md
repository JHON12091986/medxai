# AGY Agent Config

This directory contains AGY agent configuration, skills, and task definitions.

- `skills/` — reusable skill modules loaded by AGY
- `tasks/` — task queue and session state

> Renamed from `.agy/` and `.agyspec/` — single clean home for all AGY config.
