[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = . tools
norecursedirs = upgrades backups logs data .venv venv
markers =
    monkeytype: tests that depend on the monkeytype CLI tool
    smoke: basic import and functionality checks
