# NINA Claude Feed — 2026-06-24 19:03:21

## docs/space/nina_error_register.md
---
title: NINA Error & Fix Register
version: 1.0
updated: 2026-06-23
source: auto-generated by ninaflash D-06 from update_log + guardian signatures + context
note: Update row status after every fix. Append new rows, never delete old ones.
---

## Legend
🔴 BLOCKER | 🟠 WARN | 🟡 DEBT | 🔵 OPEN/PENDING | ✅ FIXED

| ID | Severity | Component | Issue (short) | Status | Assignee | Fixed In | File(s) | jules_attempts | jules_last_attempt | OPEN |
|----|----------|-----------|---------------|--------|----------|----------|---------|----------------|--------------------| OPEN |
| config.missing_env.apisecretkey | 🔴 BLOCKER | core.config | API_SECRET_KEY missing or empty in .env | PAUSED_HUMAN_ACTION_REQUIRED | unassigned | v12.2.0 | .env, core/config.py | 0 | null | OPEN |
| config.missing_env.authorizeduserid | 🔴 BLOCKER | core.config | AUTHORIZED_USER_ID missing from .env | PAUSED_HUMAN_ACTION_REQUIRED | unassigned | v12.2.0 | .env, core/config.py | 0 | null | OPEN |
| config.missing_env.telegrambottoken | 🔴 BLOCKER | core.config | TELEGRAM_BOT_TOKEN missing from .env | PAUSED_HUMAN_ACTION_REQUIRED | unassigned | v12.2.0 | .env, core/config.py | 0 | null | OPEN |
| cron.conflicting_id | 🔴 BLOCKER | crons.manager | APScheduler ConflictingIdError — duplicate job ID | PAUSED_HUMAN_ACTION_REQUIRED | unassigned | R-23 | crons/manager.py | 0 | null | OPEN |
| process.ghost_instance | 🔴 BLOCKER | process | Ghost NINA process still running | ✅ FIXED | unassigned | v12.0.0 | data/nina.pid, main.py | 0 | null | OPEN |
| process.lock_conflict | 🔴 BLOCKER | process | BlockingIOError on nina.lock — concurrent process conflict | ✅ FIXED | unassigned | v12.0.0 | data/nina.lock, main.py | 0 | null | OPEN |
| router.attr.forcelocal | 🔴 BLOCKER | core.router | NameError: forcelocal not defined (should be force_local) | ✅ FIXED | unassigned | R-70 | core/router.py, core/agent.py | 0 | null | OPEN |
| router.attr.orderedproviders | 🔴 BLOCKER | core.router | HybridRouter AttributeError: ordered_providers vs _ordere... | ✅ FIXED | unassigned | R-69 | core/router.py | 0 | null | OPEN |
| router.attr.self_http | 🔴 BLOCKER | core.router | HybridRouter AttributeError: self._http vs self.http | ✅ FIXED | unassigned | R-72 | core/router.py | 0 | null | OPEN |
| startup.attributeError | 🔴 BLOCKER | startup | AttributeError at startup/import | ✅ FIXED | unassigned | v12.2.0 | main.py | 0 | null | OPEN |
| startup.importError | 🔴 BLOCKER | startup | ImportError or ModuleNotFoundError at startup | ✅ FIXED | unassigned | v12.2.0 | main.py | 0 | null | OPEN |
| startup.nameError | 🔴 BLOCKER | startup | NameError at startup/import | ✅ FIXED | unassigned | v12.2.0 | main.py | 0 | null | OPEN |
| startup.syntaxError | 🔴 BLOCKER | startup | SyntaxError in Python source file | ✅ FIXED | unassigned | v12.2.0 | main.py | 0 | null | OPEN |
| startup.typeError | 🔴 BLOCKER | startup | TypeError at startup/import | ✅ FIXED | unassigned | v12.2.0 | main.py | 0 | null | OPEN |
| telegram.classify_task.method_not_iterable | 🔴 BLOCKER | interfaces.telegram | TypeError: 'method' object is not iterable — classify_task(text, self._local_fast) passed method ref as messages arg | ✅ FIXED | perplexity | v13.3.1 | interfaces/telegram_interface.py:474,752 | 0 | 2026-06-23 | CLOSED |
| telegram.duplicate_process | 🟠 WARN | process | Two NINA processes running (PID 111639 venv + PID 112058 .venv) | ✅ FIXED | perplexity | v13.3.1 | — | 0 | 2026-06-23 | CLOSED |
| env.openai_key_dummy | 🟡 DEBT | core.config | OPENAI_API_KEY set to literal "nina" (dummy value) — OPENAI provider non-functional | 🔵 OPEN/PENDING | unassigned | — | .env | 0 | 2026-06-23 | OPEN |
| capabilities.race_condition | 🟠 WARN | core.capabilities | Race condition on capabilities.json writes | ✅ FIXED | unassigned | D-22 | core/capabilities.py | 0 | null | OPEN |
| cron.lambda_coroutine_drop | 🟠 WARN | crons.manager | APScheduler lambda returning coroutine without await | ✅ FIXED | unassigned | D-23 | crons/manager.py | 0 | null | OPEN |
| hotreload.deleted_key_revert | 🟠 WARN | core.hotreload | Hot-reload silently ignores deleted .env keys | ✅ FIXED | unassigned | R-44 | core/hotreload.py | 0 | null | OPEN |
| logger.duplicate_handler | 🟠 WARN | core.nina | Duplicate log handler added on every restart | ✅ FIXED | unassigned | D-21 | core/nina.py | 0 | null | OPEN |
| memory.blocking_io_in_async | 🟠 WARN | core.memory | Blocking IO inside async memory methods | ✅ FIXED | unassigned | R-34 | core/memory.py | 0 | null | OPEN |
| queue.path_mismatch | 🟠 WARN | core.nina | Idle queue path mismatch: idlequeue.json vs idle_queue.json | ✅ FIXED | unassigned | R-61 | core/nina.py, tools/upgradepipeline.py | 0 | null | OPEN |
| router.cache.dict_mutation | 🟠 WARN | core.router | ResponseCache purge_expired mutates dict during iteration | ✅ FIXED | unassigned | R-58 | core/router.py | 0 | null | OPEN |
| router.fallback.no_user_safe_reply | 🟠 WARN | core.router | Router raises RuntimeError instead of user-safe fallback ... | ✅ FIXED | unassigned | R-59 | core/router.py | 0 | null | OPEN |
| telegram.document.handler_order | 🟠 WARN | interfaces.telegram | Document uploads silently dropped — handler order bug | ✅ FIXED | unassigned | D-20 | interfaces/telegram_interface.py | 0 | null | OPEN |
| telegram.key.echoed_in_chat | 🟠 WARN | interfaces.telegram | API key echoed in Telegram chat without masking | ✅ FIXED | unassigned | D-20 | interfaces/telegram_interface.py | 0 | null | OPEN |
| telegram.parsemode.badrequest | 🟠 WARN | interfaces.telegram | Telegram BadRequest caused by parse_mode=Markdown | ✅ FIXED | unassigned | D-20 | interfaces/telegram_interface.py | 0 | null | OPEN |
| browser.ssrf.guard_regression | 🟡 DEBT | tools.browser | SSRF guard uses substring matching instead of ipaddress m... | ✅ FIXED | unassigned | R-64 | tools/browser.py | 0 | null | OPEN |
| pipeline.eval_regex_weak | 🟡 DEBT | tools.upgradepipeline | Weak eval/exec/compile regex in upgrade scanner | ✅ FIXED | unassigned | R-54 | tools/upgradepipeline.py | 0 | null | OPEN |
| pipeline.no_content_type_guard | 🟡 DEBT | tools.upgradepipeline | Remote patch fetch missing Content-Type and size guard | ✅ FIXED | unassigned | R-63 | tools/upgradepipeline.py | 0 | null | OPEN |
| F-01 | ✅ FIXED | - | B-1 · Self-check pass for complex tasks | CLOSED | nina | F-01 | core/agent.py | 0 | null | CLOSED |
| F-04 | ✅ FIXED | - | C-1 · Expenditure tracker tool | CLOSED | nina | F-04 | tools/finance.py | 0 | null | CLOSED |
| F-05 | ✅ FIXED | - | C-2 · Share market monitor (DSE/CSE alerts) | CLOSED | nina | R-80 | tools/market.py | 0 | null | CLOSED |
| F-06 | 🔵 OPEN/PENDING | - | C-3 · Proactive reminder engine | OPEN | unassigned | — | core/nina.py | 0 | null | OPEN |
| F-07 | 🔵 OPEN/PENDING | - | C-4 · Email triage improvement | OPEN | unassigned | — | tools/office_mail.py | 0 | null | OPEN |
| F-08 | 🔵 OPEN/PENDING | - | C-5 · Personal knowledge base (`/remember` and `/recall`) | OPEN | unassigned | — | core/memory.py | 0 | null | OPEN |
| R-77 | ✅ FIXED | - | A-1 · Fix `parallel_route` RAM guard crash | CLOSED | unassigned | R-77 | core/router.py | 0 | null | CLOSED |
| R-78 | ✅ FIXED | - | A-3 · Fix tool grammar fragility (minimum viable guard) | CLOSED | unassigned | R-78 | core/agent.py | 0 | null | CLOSED |
| config.missing_env.telegramchatid | 🔵 OPEN/PENDING | core.config | TELEGRAMCHATID missing from .env (non-blocking) | PAUSED_HUMAN_ACTION_REQUIRED | unassigned | — | .env | 0 | null | OPEN |
| feature.ews_blocked | 🔵 OPEN/PENDING | tools.officemail | EWS email feature blocked (open issue O-02) | OPEN | unassigned | — | tools/officemail.py | 0 | null | OPEN |
| feature.playwright_blocked | 🔵 OPEN/PENDING | tools.browser | Playwright browser tool blocked (open issue O-01) | OPEN | unassigned | — | tools/browser.py | 0 | null | OPEN |
| tool.typehint.recursion | 🔴 BLOCKER | tools.enforce_type_hints | TypeHintEnforcer monkeytype recursion loop causing timeouts | ✅ FIXED | unassigned | R-79 | tools/enforce_type_hints.py, tests/test_enforce_type_hints.py | 0 | null | OPEN |
| tool.vulture.path | 🟠 WARN | tools.ninaflash_code | Vulture/Monkeytype binary FileNotFoundError in test suites | ✅ FIXED | unassigned | R-79 | tools/ninaflash_code.py, tools/enforce_type_hints.py | 0 | null | OPEN |
| test.agent.thermal | 🟠 WARN | tests.test_agent | Live hardware CPU temperature causing agent test failures | ✅ FIXED | unassigned | R-79 | tests/test_agent.py | 0 | null | OPEN |
| test.classifier.keyword | 🟡 DEBT | tests.test_task_classifier | "cat " keyword collision in task classifier test expectation | ✅ FIXED | unassigned | R-79 | tests/test_task_classifier.py | 0 | null | OPEN |
| E-nexus-01 | 🔵 OPEN/PENDING | docs.space | File deleted on every sync — root cause unconfirmed, run `git log --all --full-history -- docs/space/nexus_discoveries.md` to identify committer | OPEN | unassigned | — | docs/space/nexus_discoveries.md | 0 | null | OPEN |
| E-062 | 🔵 INFO | - | B-020 promoted READY — B-005 dependency confirmed DONE (PR #25) | CLOSED | unassigned | 2026-06-08 | - | 0 | null | OPEN |
| todo.crons_manager_py_735 | 🟡 DEBT | crons | [TODO] wire existing logic here | ✅ FIXED | unassigned | — | crons/manager.py | 0 | null | OPEN |
| todo.crons_backup_jobs_py_41 | 🟡 DEBT | crons | [TODO] wire existing logic here | ✅ FIXED | unassigned | — | crons/backup_jobs.py | 0 | null | OPEN |
| todo.tools_merge_resolver_py_184 | 🟡 DEBT | tools | [TODO] .*remove", re.IGNORECASE), | 🔵 OPEN/PENDING | unassigned | — | tools/merge_resolver.py | 0 | null | OPEN |
| todo.core_task_classifier_py_205 | 🟡 DEBT | core | [TODO] AG-69: convert to sync after confirming all callers updated | ✅ FIXED | unassigned | — | core/task_classifier.py | 0 | null | OPEN |
| ninagate.responses.stateless | 🟠 WARN | ninagate | /v1/responses shim was stateless — no session persistence | ✅ FIXED | perplexity | 2026-06-21 | ninagate/main.py | 0 | 2026-06-21 | CLOSED |
| ninagate.classify.case_mismatch | 🟡 DEBT | ninagate | _classify_safe() returned lowercase task_type; TTL_BY_TASK uses uppercase — all sessions fell back to DEFAULT_TTL=7200s | ✅ FIXED | perplexity | 2026-06-21 | ninagate/main.py | 0 | 2026-06-21 | CLOSED |

Archived FIXED entries → exports/nina_error_register_archive.md

## docs/space/jules_backlog.md
# NINA Jules Backlog
> **Last audited:** 2026-06-23 03:40 +06 | **Auditor:** Perplexity Architect Overwatch (Claude Sonnet 4.6)
> **Source:** Live repo read + PR history aibony/nina + surgical queue reconciliation
> **Rule:** IDLE stubs with body `"- Suggestion 1 / - Suggestion 2"` purged — zero signal, not tasks.

---

## AGY TASK RULES
1. One file per task — never multi-file unless atomic dependency
2. Always specify exact line numbers
3. Always include: read [file] FIRST before any change
4. Always include: verify with [exact command] after change
5. Never ask agy to diagnose — only to execute
6. If task needs diagnosis first → escalate to Perplexity handoff

---

## Legend

| Symbol | Meaning |
|--------|---------|
| ✅ DONE | Merged PR confirms full completion |
| ⚠️ PARTIAL | PR merged but follow-up gaps found |
| 🔴 P1 | Blocking / high-risk — do next |
| 🟠 P2 | Important — do this sprint |
| 🟡 P3 | Nice-to-have / low-risk |
| 📋 NEEDS SPEC | Idea exists, cannot dispatch until design decision made |

---

## Section 1 — Confirmed Done (PR-verified)

| ID | Description | PR |
|----|-------------|-----|
| E-1 | Remove unused constants in router (`STEP_BUDGETS`, `DEFAULT_MAX_STEPS`) | [#201](https://github.com/aibony/nina/pull/201) |
| E-2 | Standardize Gemini fallback model across `router.py`, `providers.json`, `bin/gemini` | [#225](https://github.com/aibony/nina/pull/225) + [#218](https://github.com/aibony/nina/pull/218) |
| E-3 | Externalize `MAX_CONCURRENT_SESSIONS`, `QUOTA_SOFT_LIMIT` to `core/config.py` | local commit |
| E-4 | Unify task classification logic into `core/task_classifier.py` | local commit |
| E-5 | Harden `jules.py` `session_end` — `try/except CalledProcessError` around `nina_sync.sh` | local commit |
| AG-N-05 | Context Injector (NinaFlash CLI) in `tools/ninaflash.py` | [#224](https://github.com/aibony/nina/pull/224) |
| AG-N-08 | Dependency cycle detector bug fix (`len(cycle) >= 2`) | [#196](https://github.com/aibony/nina/pull/196) |
| AG-N-09 | Code complexity watchdog — AST fix (`AsyncFor`, `AsyncWith`, `BoolOp`; removed `Try`) | [#225](https://github.com/aibony/nina/pull/225) |
| AG-N-10 | Symbol migration tool | [#224](https://github.com/aibony/nina/pull/224) |
| FLASH-AST | NinaFlash: `ast.get_source_segment` replaces `ast.unparse` | [#196](https://github.com/aibony/nina/pull/196) |
| FLASH-ARG | NinaFlash arg parsing — parenthesis counting handles escaped strings | [#201](https://github.com/aibony/nina/pull/201) |
| FLASH-LIST | NinaFlash `list` command + CPU telemetry caching | [#195](https://github.com/aibony/nina/pull/195) |
| SEC-CMD | Command injection fix — `os.system` → `subprocess.run(shell=False)` in `cmd_batch` | [#219](https://github.com/aibony/nina/pull/219) |
| PERF-MASK | Config pre-caching: secret values cached at init, not re-dumped on every mask call | [#222](https://github.com/aibony/nina/pull/222) |
| FLASH-STUB | NinaFlash duplicate stub cleanup in CLI router | [#196](https://github.com/aibony/nina/pull/196) |
| B-007 | Bootstrap `core/jules_guard.py` with `requires_human_action` + `check_before_jules_submit` | auto-ingested ✅ |
| B-008 | Add `jules_attempts`, `jules_last_attempt`, `status` columns to `nina_error_register.md` | auto-ingested ✅ |
| B-009 | Add `stale_pr_cleanup` cron (every 24h) to `crons/manager.py` | auto-ingested ✅ |
| B-010 | Create `.github/workflows/duplicate_pr_check.yml` | auto-ingested ✅ |
| B-011 | Add weekly `jules_audit` job to `crons/manager.py` | auto-ingested ✅ |
| GOV-IDX | Governance index auto-update + `test_exempt` reconciler | resolved ✅ |

---

## Section 2 — Partial / Needs Follow-Up

### ⚠️ P2-A — Provider Registry: Two Sources of Truth
**Gap:** `PROVIDERS_TIER1/2/3` dicts in `core/router.py` still duplicate data in `ninagate/providers.json`. The model name was unified but structural duplication was NOT removed. Silent drift risk remains.

**Jules spec:**
```
File: core/router.py + ninagate/providers.json
Read BOTH files FIRST.
Goal: Make ninagate/providers.json the SINGLE canonical provider registry.
  - Add "tier": 1|2|3 field to each provider entry in providers.json
  - Remove hardcoded PROVIDERS_TIER1/2/3 dicts from router.py
  - Add a loader in router.py: reads providers.json at startup, builds tier groups from "tier" field
  - ninagate/main.py already reads providers.json — do NOT change it
  - Do NOT change routing logic, only the data source
  - Do NOT touch .env, guardian_engine.py, interfaces/, or tests/
Verify: python3 -c "from core.router import Router; r=Router(); print(r.ALL_PROVIDERS)"
```

---

### ⚠️ P2-B — `session_end` Sync Failure: Silent Error (PARTIAL FIX)
**Gap:** `CalledProcessError` is caught but only logged locally. If `nina_sync.sh` fails, the Telegram user gets no alert and the session ledger may not close cleanly.

**Jules spec:**
```
File: tools/jules.py — function: session_end
Read tools/jules.py FIRST.
Goal: When nina_sync.sh raises CalledProcessError OR returns non-zero:
  - Append failure + full stderr to nina_update_log.md via append_log()
  - Send Telegram alert: "⚠️ NINA sync failed after Jules session_end. Check logs."
  - Do NOT abort session_end — sync failure is non-fatal, session must still close
  - Do NOT touch any file outside tools/jules.py
Verify: simulate failure with bad path; confirm log entry + Telegram message fires
```

---

### ⚠️ P3-A — Governance: 3 Files Unmanaged in Index
**Gap:** `docs/space/gemini_shim_spec.md`, `docs/space/jules_pipeline_audit.md`, `core/task_classifier.py` missing from governance index. False-positive warnings in pre-push hook.

**agy task (not Jules):**
```
Run tools/update_index.py locally.
Add entries for the 3 files above.
Verify: pre-push hook runs clean with no unmanaged-file warnings.
```

---

## Section 3 — Open / Pending (Not Yet Started)

### 🔴 P1-1 — Provider Health Monitoring
**Risk:** Silent quality degradation — if Gemini quota exhausts, router falls to Tier 3 with no user visibility.

**Jules spec:**
```
File: tools/provider_health.py (create new)
Read core/router.py FIRST to understand provider structure.
Goal: ProviderHealthTracker class —
  - Tracks last N=20 calls per provider: success/failure + latency
  - Exposes health_summary() → {provider: {ok_rate, avg_latency, last_error}}
  - On 3 consecutive failures: Telegram alert "⚠️ Provider {name} degraded."
  - On recovery (1 success after failure streak): log recovery event
  - Persist health state to data/provider_health.json (overwrite each update)
  - Tracker is OBSERVABILITY ONLY — do NOT change routing logic
  - Do NOT touch .env, interfaces/, guardian_engine.py, crons/
Verify: pass a bad provider key; confirm Telegram alert fires within 60s
```

---

### 🔴 P1-2 — `tools/__init__.py` `__all__` Auto-Verification
**Risk:** New `tools/*.py` modules silently invisible to agy's tool registry if not registered in `__all__`.

**Jules spec:**
```
File: tools/validate_index.py (extend existing) OR checks/verify_exports.py (create new)
Read tools/__init__.py AND tools/validate_index.py FIRST.
Goal: CI check that all tools/*.py modules (excl. __init__.py) are in tools/__init__.py __all__:
  - Parse __all__ from __init__.py using AST (not regex)
  - Scan tools/ for all *.py files (exclude __init__.py)
  - For any module not in __all__: print error and exit(1)
    "ERROR: tools/{module}.py is missing from tools/__init__.py __all__"
  - Hook into existing pre-push or validate_index run
  - Do NOT change router.py, core files, or .env
Verify: temporarily remove one module from __all__; confirm script exits 1 with correct message
```

---

### 🟠 P2-1 — NinaGate System Template → Jules Dispatch
**Risk:** Jules PRs have no NINA identity framing. Generated code/PRs don't know NINA conventions unless manually included.

**Jules spec:**
```
File: tools/jules.py — function: run_dispatch
Read tools/jules.py AND ninagate/system_templates.json FIRST.
Goal: Prepend NINA system template to every Jules dispatch prompt:
  - Read ninagate/system_templates.json at dispatch time (NOT at import time)
  - Extract "system" role content
  - Build: f"[NINA CONTEXT]\n{system_content}\n\n[TASK]\n{prompt}"
  - Cap total prompt at 8000 chars — truncate from BOTTOM of template, never the task
  - Do NOT change ninagate/main.py, core/router.py, .env, guardian_engine.py, or interfaces/
Verify: dispatch test task; confirm Jules PR description references NINA conventions
```

---

### 🟠 P2-2 — juleslock.txt Pre-Push Enforcement
**Risk:** If a Jules PR is in-flight and a manual commit touches the same locked file, the push succeeds silently.

**Jules spec:**
```
File: .git/hooks/pre-push (extend existing hook script)
Read .git/hooks/pre-push AND juleslock.txt FIRST.
Goal: Before allowing push, check if any diffed file matches a juleslock.txt entry:
  - Read juleslock.txt line by line
  - Get list of changed files: git diff --name-only origin/main HEAD
  - If any changed file is in juleslock: abort with message:
    "BLOCKED: {filename} is locked in juleslock.txt (Jules session in flight).
     Wait for Jules PR or clear the lock manually."
  - If no match: proceed normally
  - Do NOT change validate_index.py or audit_repo_hygiene.py
Verify: lock a file in juleslock.txt; push a commit touching it; confirm push blocked
```

---

### 🟠 P2-3 — `nina_sync.sh` SyntaxError in Doc Audit One-Liner
**Risk:** OODA loop hook fires broken on every git commit. Blocks all sync-dependent flows.
**⚠️ MUST FIX BEFORE ANY JULES DISPATCH — agy task, not Jules**

**agy task:**
```
SURGICAL: nina_sync.sh doc-audit one-liner
Read nina_sync.sh FIRST. Find the Python one-liner used for doc audit.
Replace with a properly quoted, multi-line safe equivalent.
Do NOT touch: core/router.py, .env, any file outside nina_sync.sh
Verify: bash -n nina_sync.sh
```

---

### 🟠 P2-4 — `gemini_shim_spec.md` → Spec-to-Code Drift Audit
**Context:** `docs/space/gemini_shim_spec.md` exists but is not in the governance index. No audit confirms `bin/gemini` matches the spec.

**Action before Jules task:**
```
Perplexity (Architect Overwatch) to read docs/space/gemini_shim_spec.md
and bin/gemini side-by-side. Identify delta between spec and implementation.
Only then write a Jules spec for the exact diff.
Status: 📋 NEEDS SPEC — do not dispatch until Perplexity reads both files.
```

---

### 🟡 P3-1 — OpenRouter Fallback: Explicit Model Pin
**Context:** Tier 3 OpenRouter entry has no pinned model — OpenRouter's default can change silently.
**Dependency:** Complete P2-A (provider registry unification) first.

**Fix after P2-A:**
```
In ninagate/providers.json, add "model": "openai/gpt-4o-mini" (or agreed equivalent)
to the OpenRouter entry. Single-line change. agy task.
Verify: python3 -c "import json; d=json.load(open('ninagate/providers.json')); print([p for p in d if 'openrouter' in p.get('id','').lower()])"
```

---

### 🟡 P3-2 — ARCHITECTURE.md Drift Update
**Context:** 7 PRs since last doc update changed `router.py`, `ninagate/main.py`, `tools/jules.py`, added `core/task_classifier.py`. ARCHITECTURE.md not updated.

**agy task (not Jules):**
```
Read ARCHITECTURE.md + core/task_classifier.py + core/config.py + tools/jules.py FIRST.
Update ARCHITECTURE.md to cover:
  - Unified classifier (task_classifier.py)
  - Externalised config (MAX_CONCURRENT_SESSIONS, QUOTA_SOFT_LIMIT)
  - jules session_end hardening
Single file, append/replace affected sections only.
Verify: Read the file and confirm sections are accurate.
```

---

## Section 4 — Agent Framework Tasks (Surgical Queue — Active)

These are implementation tasks for `core/agent.py`, `core/jules_guard.py`, and related files. Each has been reviewed and is ready to dispatch in the order listed. Dispatch one at a time; verify before next.

### P-03 — Guard `check_before_jules_submit` in `evolve_loop.py`
```
File: crons/evolve_loop.py — lines 50-80
Read crons/evolve_loop.py AND core/jules_guard.py FIRST.
Action: Wrap every Jules task submission with:
  if check_before_jules_submit(error_id, open_prs): skip
  Log all skips to docs/space/jules_skip_log.md (append-only)
Do NOT touch core/router.py
Verify: python3 -m py_compile crons/evolve_loop.py
```

### P-12 — First Weekly Audit Report
```
File: docs/space/jules_weekly_audit.md (create if absent)
Read docs/space/jules_weekly_audit.md FIRST.
Action: Create table with pass/fail status of Jules PR pipelines from last 7 days.
Do NOT touch core/router.py
Verify: read the file
```

### P-14 — Perplexity Handoff Protocol Template
```
File: docs/space/perplexity_handoff.md (create if absent)
Action: Create file with Perplexity handoff template:
  - Fields: task_id, error_id, context_files[], description, expected_outcome
Do NOT touch core/router.py
Verify: read the file
```

### P-15 — Pre-load Context Injector for agy
```
File: core/agy_briefing.py (create if absent)
Action: Create generate_agy_brief(file_paths: list, task: str) -> str
  - Extracts relevant context from listed files, cap 2000 chars
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agy_briefing.py
```

### P-17 — agy Memory Anchor on Session Start
```
File: core/agent.py — lines 100-130
Read core/agent.py FIRST.
Action: Inject MEMORY ANCHOR at session start reading:
  perplexity_handoff.md, nina_error_register.md, jules_backlog.md
  Re-read if messages > 50 in session.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-20 — Skip Pipeline Overhead for SURGICAL Tasks
```
File: core/agent.py — lines 200-240
Read core/agent.py FIRST.
Action: In _inner(), if task starts with "SURGICAL:" prefix,
  skip steps 1-11 and jump directly to execution.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-21 — Mid-Process Critic Checkpoints
> ⚠️ **CORRECTED 2026-06-22:** Target is `core/agent.py → _self_check()`, NOT the dead
> `core/cognitive/evaluator.py` (file does not exist in live repo).
```
File: core/agent.py — function: _self_check()
Read core/agent.py FIRST. Locate _self_check() — do NOT create evaluator.py.
Action: Add lightweight mid-process critic inside _self_check():
  - Check if files were read before edits were attempted
  - Log violations to docs/space/agy_process_log.md (append-only)
Do NOT touch core/router.py or create core/cognitive/evaluator.py
Verify: python3 -m py_compile core/agent.py
```

### P-22 — Persistent Context Anchors (PINNED_CONTEXT)
```
File: core/agent.py — lines 250-280
Read core/agent.py FIRST.
Action: Implement PINNED_CONTEXT prepended to every LLM call in session.
  Max 800 tokens. Content: nina_runbook.md (once created).
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-23 — Skip feedback_prompt on Read-Only Operations
```
File: core/agent.py — lines 300-330
Read core/agent.py FIRST.
Action: In run_tool_with_fix(), skip feedback_prompt for read-only tool calls.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-24 — DEBUG Structured Protocol Sequence
```
File: core/agent.py — lines 350-400
Read core/agent.py FIRST.
Action: Under DEBUG: prefix, enforce strict sequence:
  read → FINDINGS → ROOT_CAUSE → SURGICAL_FIX → execute → verify
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-25 — Session Continuity Summary
```
File: core/agent.py — lines 410-450
Read core/agent.py FIRST.
Action: Generate SESSION_SUMMARY every 20 messages.
  Compress oldest context while preserving summary.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/agent.py
```

### P-27 — Loop Detection in jules_guard.py
```
File: core/jules_guard.py — lines 40-70
Read core/jules_guard.py FIRST.
Action: Implement detect_loop(error_id):
  - Detect if same error_id dispatched > 3 times with no merge
  - Send Telegram notification on loop detection
Do NOT touch core/router.py
Verify: python3 -m py_compile core/jules_guard.py
```

### P-28 — Incident Runbook
```
File: docs/space/nina_runbook.md (create if absent)
Action: Create runbook with incident response rules (provider down, sync fail, loop detected).
  After creation: add as PINNED_CONTEXT source in core/agent.py (P-22).
Do NOT touch core/router.py
Verify: read the file
```

### P-29 — Dead Branch Cleanup Workflow
```
File: .github/workflows/dead_branch_cleanup.yml (create if absent)
Action: GitHub Actions cron that deletes branches merged > 14 days ago with no activity.
Do NOT touch core/router.py
Verify: YAML lint
```

### P-30 — Error Register Auto-Sync
```
File: tools/error_register_sync.py (create if absent)
Action: Scan codebase for TODO/FIXME comments; sync new entries into nina_error_register.md.
Do NOT touch core/router.py
Verify: python3 -m py_compile tools/error_register_sync.py
```

### P-31 — Jules Task Spec Validator
```
File: core/jules_guard.py — lines 80-120
Read core/jules_guard.py FIRST.
Action: Add validate_task_spec(task: str) -> dict:
  - Enforce: single file only, line numbers present, < 500 words, no secrets/env vars
  - Return {valid: bool, errors: list}
Do NOT touch core/router.py
Verify: python3 -m py_compile core/jules_guard.py
```

### P-32 — Rollback Registry Workflow
```
File: .github/workflows/jules_merge_guard.yml (create if absent)
Action: On PR merge: tag commit as rollback anchor; write entry to docs/space/rollback_registry.md.
Do NOT touch core/router.py
Verify: YAML lint
```

### P-34 — Jules Daily Dispatch Rate Limit
```
File: core/jules_guard.py — lines 130-150
Read core/jules_guard.py FIRST.
Action: Add daily dispatch counter in nina_state.json.
  Block dispatch if count >= MAX_DAILY_DISPATCHES (default 10).
Do NOT touch core/router.py
Verify: python3 -m py_compile core/jules_guard.py
```

### P-35 — Pre-Merge Test Gate
```
File: .github/workflows/jules_merge_guard.yml — lines 51-100
Read the file FIRST.
Action: Enforce pytest passes before merge is allowed. Block merge on test failure.
Do NOT touch core/router.py
Verify: YAML lint
```

### P-36 — Secrets Detection in jules_guard.py
```
File: core/jules_guard.py — lines 160-200
Read core/jules_guard.py FIRST.
Action: Extend requires_human_action() to scan task spec for secrets patterns:
  (API key regex, env var names, token patterns). Block dispatch if found.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/jules_guard.py
```

### P-37 — NINA Self-Health Cron
```
File: crons/manager.py — lines 160-200
Read crons/manager.py FIRST.
Action: Add nina_healthcheck job running every 6 hours.
  Updates nina_state.json with {timestamp, service_status, last_sync, provider_tier}.
Do NOT touch core/router.py
Verify: python3 -m py_compile crons/manager.py
```

### P-38 — Append-Only Audit Ledger
```
File: docs/space/nina_audit_log.jsonl (create if absent) + core/jules_guard.py
Read core/jules_guard.py FIRST.
Action: Create nina_audit_log.jsonl; add append_audit(entry: dict) in jules_guard.py.
  Called on every dispatch, loop detection, and block event.
Do NOT touch core/router.py
Verify: python3 -m py_compile core/jules_guard.py
```

---

## Section 4b — Architecture Tasks (core/agent.py Deep Audit)

> Added: 2026-06-22 15:56 +06 | Source: Perplexity Architect Overwatch deep audit of `core/agent.py`
> Dispatch order: ARCH-1 → ARCH-2 → ARCH-3 (needs spec first) → ARCH-4 (needs pre-check)

### 🔴 ARCH-1 — Orphan Rename (agy task, not Jules)
**Priority:** P1 | **Owner:** agy
**Context:** `core/agent.py` references at least one symbol under its old name (pre-refactor rename). The orphan name is imported/used but the canonical definition has moved. Runtime `AttributeError` or silent wrong-object usage risk.

```
agy task — SURGICAL
Read core/agent.py FIRST. Grep for any symbol that resolves to a dead import path.
Goal: Rename all orphan references to match the live canonical symbol name.
  - Do NOT rename the canonical definition itself
  - Do NOT touch any file outside core/agent.py
  - If the orphan appears in tests, update test import only (separate task)
Verify: python3 -c "from core.agent import Agent; print('import OK')"
```

---

### 🟠 ARCH-2 — Ghost Layer Archive (Jules task)
**Priority:** P2 | **Owner:** Jules
**Context:** `core/agent.py` contains a "ghost layer" — a class or function block that was partially refactored out but left in place (dead body, no callers, no tests). Leaves noise in the module and misleads future readers.

**Jules spec:**
```
File: core/agent.py
Read core/agent.py FIRST. Identify the ghost layer block (class or function with no callers).
Goal: Move the ghost block to archive/orphan_cognitive/ (do not delete — preserve for archaeology).
  - Remove import of the ghost class/function from core/agent.py
  - Update any __all__ entry if present
  - Do NOT change any live class, function, or caller
  - Do NOT touch core/router.py, .env, or tests/
Verify:
  python3 -m py_compile core/agent.py
  grep -n "<ghost_symbol_name>" core/agent.py  # must return 0 results
```

---

### 🔴 ARCH-3 — `goal_manager` Implementation  📋 NEEDS SPEC
**Priority:** P1 | **Owner:** Needs design decision before dispatch
**Context:** `core/agent.py` references `self.goal_manager` in multiple methods but the attribute is never initialised in `__init__`. Every code path that touches `goal_manager` is currently a silent `AttributeError` at runtime.

**⚠️ Do NOT dispatch to Jules until spec is written.**
**Decision needed (Perplexity to resolve):**
- Is `goal_manager` a thin wrapper around the existing task queue, or a new stateful goal stack?
- Where does it persist state — `nina_state.json`, a new `data/goals.json`, or in-memory only?
- Which methods in `agent.py` call it and what is the expected interface (`add_goal`, `pop_goal`, `current_goal`)?

→ Once decided, Perplexity will write the Jules spec with exact `__init__` wiring and method stubs.

```
Status: 📋 NEEDS SPEC — do not dispatch until Perplexity reads core/agent.py and writes design.
```

---

### 🟡 ARCH-4 — `knowledge_graph` Wiring  📋 NEEDS PRE-CHECK
**Priority:** P3 | **Owner:** Needs pre-check before dispatch
**Context:** `core/agent.py` references `self.knowledge_graph` (read + write calls) but the attribute initialisation path is conditional — it may not be set on all code paths, creating a latent `AttributeError` on cold-start or when the knowledge graph feature flag is off.

**⚠️ Pre-check required before dispatch:**
```
Perplexity (Architect Overwatch) to:
  1. Read core/agent.py — find all knowledge_graph initialisation points
  2. Read tools/knowledge_graph.py (or equivalent) — confirm the class interface
  3. Determine if this is a flag-gated feature or always-on
  4. Only then write a Jules spec for safe unconditional wiring in __init__
Status: 📋 NEEDS PRE-CHECK — Perplexity must read both files first.
```

---

## Section 5 — Needs Spec (Design Decision Required)

### 📋 SPEC-1 — Cron Health Dashboard
**Context:** `crons/manager.py`, `crons/evolve_loop.py`, `crons/backup_jobs.py` run on schedule with no Telegram-visible status. If `evolve_loop.py` silently fails, NINA's self-improvement loop stops with no alert.
**Decision needed:** Should this be a `/cron_status` Telegram command or a static file written to `docs/space/`?
→ Once decided, Perplexity will write the Jules spec.

### 📋 SPEC-2 — Session Ledger Integrity Audit
**Context:** `tools/session_ledger.py` tracks session history but no periodic check for orphaned sessions (started, never closed) or sessions where `nina_sync.sh` failed.
**Decision needed:** What is the "orphaned session" threshold? (e.g., open > 2 hours with no activity?)
→ Once decided, Perplexity will write the Jules spec.

### 📋 SPEC-3 — NINA Observability Dashboard (P-26)
**Context:** Static HTML dashboard rendering error counts, PR rates, system health with auto-refresh. Spec exists (P-26) but the data sources and refresh mechanism need a design decision.
**Decision needed:** Pull from `data/provider_health.json` + `nina_state.json`? Auto-refresh interval?
→ Once decided, Jules-ready spec can be written.

---

## Section 6 — v2 ASI Audit Tasks (2026-06-23)
> Source: NINA ASI Deep Audit Report v2 | Auditor: Perplexity Architect Overwatch
> All 10 tasks Jules-ready. Dispatch in order: AU-01 → AU-02 → AU-03 (highest ROI first).

### 🔴 AU-01 — Merge `hyperdrive_cache.py` into `memo_cache.py`
**ROI:** High | **Owner:** Jules
**Context:** `core/hyperdrive_cache.py` overlaps with `core/memo_cache.py` (semantic + tool-output caching vs general memoization). Two cache systems = silent divergence risk.

**Jules spec:**
```
Files: core/hyperdrive_cache.py + core/memo_cache.py
Read BOTH files FIRST. Map every public method in hyperdrive_cache.py.
Goal: Merge unique methods from hyperdrive_cache.py into memo_cache.py.
  - Do NOT duplicate existing memo_cache.py methods
  - Update all importers of hyperdrive_cache.py to import from memo_cache.py
    grep -rn "hyperdrive_cache" core/ tools/ agents/ --include="*.py"
  - Delete core/hyperdrive_cache.py after all imports are migrated
  - Do NOT touch core/router.py, .env, guardian_engine.py, interfaces/
Verify:
  python3 -m py_compile core/memo_cache.py
  grep -rn "hyperdrive_cache" . --include="*.py"  # must return 0
```

---

### 🔴 AU-02 — Extend `git-hooks/post-commit` to update `nina_index.json`
**ROI:** High | **Owner:** Jules
**Context:** Post-commit hook triggers `guardian_loop` but does NOT call `tools/update_index.py`. SSOT index goes stale after every commit — semantic router reads outdated metadata.

**Jules spec:**
```
File: git-hooks/post-commit
Read git-hooks/post-commit FIRST. Identify current hook body.
Goal: After existing guardian_loop call, append:
  python3 tools/update_index.py --changed-only 2>> /tmp/nina_hook.log &
  - Use --changed-only flag if supported; otherwise full reindex
  - Run in background (&) — do NOT block the commit
  - Stderr redirected to /tmp/nina_hook.log, not stdout
  - Do NOT touch core/router.py, .env, or any Python file
Verify:
  bash -n git-hooks/post-commit
  Make a test commit; confirm docs/space/nina_index.json mtime updates
```

---

### 🔴 AU-03 — Add `@memo_cache` decorator to `core/memo_cache.py`
**ROI:** High | **Owner:** Jules
**Context:** `memo_cache.py` exists but has no decorator. Every LLM call site re-implements caching inline or skips it entirely.

**Jules spec:**
```
File: core/memo_cache.py
Read core/memo_cache.py FIRST.
Goal: Add memo_cache decorator:
  def memo_cache(ttl=604800):  # 7-day default
    def decorator(fn):
      @functools.wraps(fn)
      def wrapper(*args, **kwargs):
        key = hashlib.sha256((str(args) + str(kwargs)).encode()).hexdigest()
        cached = MemoCache().get(key)
        if cached: return cached
        result = fn(*args, **kwargs)
        MemoCache().set(key, result, ttl=ttl)
        return result
      return wrapper
    return decorator
  - Export decorator in __all__
  - Do NOT change existing MemoCache class methods
  - Do NOT touch core/router.py, .env, or any other file
Verify:
  python3 -m py_compile core/memo_cache.py
  python3 -c "from core.memo_cache import memo_cache; print('decorator OK')"
```

---

### 🟠 AU-04 — Replace keyword scoring with embeddings in `context_compressor.py`
**ROI:** High | **Owner:** Jules
**Context:** `core/context_compressor.py` uses keyword-based relevance scoring. `core/semantic_router.py` already has `sentence-transformers` loaded — same model can score chunks.

**Jules spec:**
```
File: core/context_compressor.py
Read core/context_compressor.py AND core/semantic_router.py FIRST.
Goal: Replace keyword scoring with cosine similarity via sentence-transformers:
  - Import SemanticRouter (or its model) from core/semantic_router.py — do NOT load a second model
  - Replace keyword_score(chunk, query) with embed_score(chunk, query) using cosine_similarity
  - Keep AST extraction logic unchanged
  - Cache compressed chunks to data/compressed_cache/{hash}.pkl
    Create data/compressed_cache/ if absent
  - Do NOT touch core/router.py, .env, guardian_engine.py
Verify:
  python3 -m py_compile core/context_compressor.py
  python3 -c "from core.context_compressor import ContextCompressor; print('OK')"
```

---

### 🟠 AU-05 — Implement `scripts/idempotency_check.py`
**ROI:** High | **Owner:** Jules
**Context:** No idempotency checks exist. Scripts and hooks can fire duplicate side effects on re-run.

**Jules spec:**
```
File: scripts/idempotency_check.py (create new)
Goal: IdempotencyChecker class with:
  - mark(op_id: str) → writes op_id + timestamp to data/idempotency_ledger.json
  - already_done(op_id: str) → bool, checks ledger
  - clear(op_id: str) → removes entry (for reset/retry flows)
  - CLI: python3 scripts/idempotency_check.py --dry-run <op_id>
    prints already_done result without writing
  - Ledger is append-only JSON dict {op_id: {ts, count}}
  - Do NOT touch core/router.py, .env, guardian_engine.py
Verify:
  python3 -m py_compile scripts/idempotency_check.py
  python3 scripts/idempotency_check.py --dry-run test_op_001
```

---

### 🟠 AU-06 — Create `data/example_bank/` for dynamic few-shot prompting
**ROI:** High | **Owner:** Jules
**Context:** No example bank exists. LLM calls use zero-shot only — dynamic few-shot can significantly improve output quality for recurring task types.

**Jules spec:**
```
Files: data/example_bank/ (create dir) + core/few_shot.py (create new)
Goal:
  1. Create data/example_bank/ with example schema file:
     data/example_bank/schema.json → {"task_type": str, "input": str, "output": str, "embedding": null}
  2. Seed with 3 example entries for common NINA task types (code_fix, doc_update, route_query)
     Save as data/example_bank/seed_examples.json
  3. Create core/few_shot.py:
     - get_examples(query: str, task_type: str, k=3) -> list[dict]
     - Loads examples from data/example_bank/seed_examples.json
     - Ranks by string similarity (difflib) until embeddings are wired
     - Returns top-k as {input, output} pairs for prompt injection
  - Do NOT touch core/router.py, .env, guardian_engine.py
Verify:
  python3 -m py_compile core/few_shot.py
  python3 -c "from core.few_shot import get_examples; print(get_examples('fix router bug', 'code_fix'))"
```

---

### 🟡 AU-07 — Archive ghost files to `archive/`
**ROI:** Medium | **Owner:** Jules
**Context:** 4 confirmed ghost files in `core/` — zero callers, zero tests: `autogen.py`, `ast_refactor.py`, `canonicalization.py`, `gap_analysis.py`.

**Jules spec:**
```
Files: core/autogen.py, core/ast_refactor.py, core/canonicalization.py, core/gap_analysis.py
Read each file FIRST. Confirm zero callers:
  grep -rn "autogen\|ast_refactor\|canonicalization\|gap_analysis" . --include="*.py" | grep -v "^core/<filename>"
Goal:
  - Create archive/ghost_core/ if absent
  - Move all 4 files to archive/ghost_core/
  - Remove any __init__.py imports of these modules
  - Do NOT delete files — preserve for archaeology
  - Do NOT touch core/router.py, .env, guardian_engine.py
Verify:
  ls archive/ghost_core/  # 4 files present
  python3 -c "import core"  # no ImportError
```

---

### 🟡 AU-08 — Wire `token_counter.py` into `core/router.py`
**ROI:** Medium | **Owner:** Jules
**Context:** `core/token_counter.py` exists (partial per v1 audit) but is not called before LLM calls in `core/router.py` (line 189). Token budget blindness causes silent context overflow.

**Jules spec:**
```
File: core/router.py — around line 189
Read core/router.py AND core/token_counter.py FIRST.
Goal: Before every LLM dispatch call in router.py:
  - Call token_counter.count(prompt) → int
  - If count > model_context_limit: truncate prompt tail to fit, log truncation
  - Import TokenCounter from core/token_counter.py at top of file
  - Do NOT change routing logic, provider selection, or failover
  - Do NOT touch .env, guardian_engine.py, interfaces/
Verify:
  python3 -m py_compile core/router.py
  python3 -c "from core.router import Router; print('import OK')"
```

---

### 🟡 AU-09 — Add `purpose` and `owner` fields to `nina_index.json`
**ROI:** Medium | **Owner:** Jules
**Context:** `docs/space/nina_index.json` generated by `tools/update_index.py` is missing `purpose` and `owner` fields per v2 audit. Semantic router cannot filter by ownership.

**Jules spec:**
```
File: tools/update_index.py
Read tools/update_index.py AND CODEBASE_MAP.md FIRST.
Goal: Extend update_index.py to populate purpose and owner fields:
  - Parse CODEBASE_MAP.md: extract **Purpose:** and **Owner:** lines per file entry
  - For files not in CODEBASE_MAP.md: set purpose="UNKNOWN", owner="UNKNOWN"
  - Add both fields to every entry in nina_index.json output
  - Do NOT change existing field structure — only ADD new fields
  - Do NOT touch core/router.py, .env, guardian_engine.py
Verify:
  python3 -m py_compile tools/update_index.py
  python3 tools/update_index.py
  python3 -c "import json; d=json.load(open('docs/space/nina_index.json')); print(d[list(d)[0]].get('purpose'))"
```

---

### 🟡 AU-10 — Systematically replace duplicate configs with symlinks
**ROI:** Low | **Owner:** agy (not Jules — filesystem ops)
**Context:** Multiple agents have duplicate config copies. `tools/AGENTS.md` already a symlink. Pattern should be extended to all duplicated config/prompt files.

**agy task:**
```
SURGICAL: config symlink audit
Run: find agents/ tools/ -name "*.md" -o -name "*.json" | xargs md5sum | sort | uniq -D -w32
For each duplicate pair:
  - Keep canonical copy at repo root or docs/context/
  - Replace duplicate with: ln -sf <canonical_path> <duplicate_path>
  - Verify symlinks resolve: ls -la <symlink_path>
Do NOT create symlinks for .env, secrets, or runtime-written files.
Verify: find . -type l | xargs ls -la
```

---

## Summary Snapshot
> Updated: 2026-06-23 03:40 +06

| Category | Count |
|----------|-------|
| ✅ Confirmed done | 20 |
| ⚠️ Partial — follow-up needed | 3 |
| 🔴 P1 — do next | 6 (incl. ARCH-1, ARCH-3, AU-01, AU-02, AU-03) |
| 🟠 P2 — this sprint | 8 (incl. ARCH-2, AU-04, AU-05, AU-06) |
| 🟡 P3 — low priority | 6 (incl. ARCH-4, AU-07, AU-08, AU-09, AU-10) |
| Agent framework (P-series) | 18 surgical tasks |
| Architecture (ARCH-series) | 4 tasks |
| v2 Audit (AU-series) | 10 tasks |
| 📋 Needs spec / pre-check | 5 |
| 🗑️ IDLE stubs purged | ~50+ |

**Recommended next session order:**
1. **agy now:** ARCH-1 orphan rename (unblocks all agent.py work)
2. **agy now:** Fix `nina_sync.sh` SyntaxError (P2-3) — unblocks OODA loop
3. **Jules:** AU-01 merge hyperdrive_cache → memo_cache
4. **Jules:** AU-02 extend post-commit hook → update nina_index.json
5. **Jules:** AU-03 add @memo_cache decorator
6. **Perplexity:** Read `core/agent.py` → spec ARCH-3 `goal_manager` design
7. **Jules:** AU-04 context_compressor embeddings + AU-05 idempotency_check
8. **Jules:** AU-06 example_bank + few_shot.py
9. **Jules:** ARCH-2 ghost layer archive + AU-07 ghost files
10. **Jules:** P1-1 + P1-2 + P2-A in parallel
| B-012 | [Auto Betterment Task] Betterment_TITLE: Improve missing env | `READY` | TBD | — | Auto-ingested via goal_to_backlog |

---

### IDLE-20260623-1118 | Auto-Promoted by IdleLoop | Morning Report

**Status:** READY
**Impact:** High
**Source:** idleloop auto-promotion — 2026-06-23 11:18
**Topic:** morning_report

**Analysis:**
- Suggestion 1
- Suggestion 2

**Acceptance Criteria (Jules must verify):**
- Implement the highest-priority suggestion from the analysis above
- Touch only the file(s) explicitly named in the analysis
- Do NOT touch: `core/router.py`, `core/nina.py`, `guardian_engine.py`, `interfaces/telegram_interface.py`
- Guardian must PASS before PR is opened
- Add a log statement proving the fix is active at runtime
| B-013 | [Betterment TITLE: Fix missing environment variables

 Confi | `READY` | TBD | — | Auto-ingested via goal_to_backlog |
| B-014 | [Improve .env file validation] Enhance core.config.py to ens | `READY` | TBD | — | Auto-ingested via goal_to_backlog |

---

### IDLE-20260624-1329 | Auto-Promoted by IdleLoop | Tool Error Handling

**Status:** READY
**Impact:** High
**Source:** idleloop auto-promotion — 2026-06-24 13:29
**Topic:** tool_error_handling

**Analysis:**
1. **Exact File + Function:**
   - **agent/__init__.py** / `__main__`
   
2. **What Can Go Wrong:**
   - The root cause of this issue could be the incorrect handling or execution of initialization logic for the agent module itself.
   
3. **Suggested Fix in Plain English:**
   - Ensure that all necessary imports and configurations are correctly set up within the `__init__.py` file, especially if it serves as a parent class for other modules or initializes dependencies. Review any new scripts introduced to ensure they integrate seamlessly with the agent's initialization process without causing unexpected errors.

This high impact indicates potential issues in the foundational setup of NINA’s agents and their interactions with system-level infrastructure.

**Acceptance Criteria (Jules must verify):**
- Implement the highest-priority suggestion from the analysis above
- Touch only the file(s) explicitly named in the analysis
- Do NOT touch: `core/router.py`, `core/nina.py`, `guardian_engine.py`, `interfaces/telegram_interface.py`
- Guardian must PASS before PR is opened
- Add a log statement proving the fix is active at runtime


---

### IDLE-20260624-1401 | Auto-Promoted by IdleLoop | Morning Report

**Status:** READY
**Impact:** High
**Source:** idleloop auto-promotion — 2026-06-24 14:01
**Topic:** morning_report

**Analysis:**
1. Missing a comprehensive error handling mechanism in the `run_morning_report` function.
   - Why it matters:
     The morning report generator is responsible for processing various reports and scripts, including some that can fail due to network issues or incorrect configurations.
     If these errors go unhandled, they could lead to incomplete or inaccurate reports being generated, which might result in decision-making based on outdated information.

2. Fragile dependency management.
   - Why it matters:
     The script depends on several external tools and modules (e.g., `nina`, `agents`, etc.). If any of these dependencies are not installed correctly or are unavailable at runtime, the morning report generation could fail completely.

3. Lack of monitoring and logging.
   - Why it matters:
     Without proper monitoring and logging mechanisms, issues that arise during the execution of the morning report will go unnoticed. This can lead to a high chance of errors going unnoticed until they result in significant problems downstream.

4. Missing unit tests for key functionalities.
   - Why it matters:
     Unit testing ensures that each component of the system works as expected without any bugs or failures. Lacking such tests means that issues might only be discovered during production runs, leading to potential downtime and increased complexity when fixing bugs.

5. Poor documentation.
   - Why it matters:
     Lack of clear documentation makes it difficult for new users or maintenance staff to understand how the morning report works. This can lead to inefficiencies in using the system and potentially cause errors if misconfigured settings are used inadvertently.

Suggested fix:

1. Implement a robust error handling mechanism that checks dependencies before executing critical parts of `run_morning_report`.
2. Ensure all required tools/modules are correctly installed and configured.
3. Add comprehensive logging throughout the script to track runtime issues.
4. Write unit tests for key functionalities using a testing framework like Pytest or unittest.
5. Enhance documentation with clear instructions on how to set up dependencies and configure settings.

**Acceptance Criteria (Jules must verify):**
- Implement the highest-priority suggestion from the analysis above
- Touch only the file(s) explicitly named in the analysis
- Do NOT touch: `core/router.py`, `core/nina.py`, `guardian_engine.py`, `interfaces/telegram_interface.py`
- Guardian must PASS before PR is opened
- Add a log statement proving the fix is active at runtime
| B-015 | [Improve API Secret Key Configuration] 1. Add a check in `co | `READY` | TBD | — | Auto-ingested via goal_to_backlog |

## docs/space/nina_runbook.md
# Nina Runbook — Single Source of Truth
_auto-maintained · last updated by OODA loop_

## One Command to Rule All
```bash
bash scripts/nina_sync.sh
```
This is the ONLY command any agent (Jules, agy, Gemini, Cursor, OpenCode, Perplexity, cron) needs to call. It runs a full OODA loop and handles everything below automatically.

## OODA Loop Phases

| Phase | What happens |
|---|---|
| **OBSERVE** | Snapshot SHA, count open PRs, dirty files, stash depth |
| **ORIENT** | Safe-pull (stash→rebase×3→pop), hooks reinstall, detect what changed |
| **DECIDE** | Flag: need index regen? doc audit? dead-code scan? dup check? service restart? |
| **ACT** | Execute only flagged actions → commit artefacts → push → restart service |

## Modes
```bash
bash scripts/nina_sync.sh            # full OODA (default)
bash scripts/nina_sync.sh pull-only  # safe pull + hooks, no push
bash scripts/nina_sync.sh push-only  # commit + push, skip pull
bash scripts/nina_sync.sh audit      # deep scan (dead-code + dups), no push
bash scripts/nina_sync.sh hooks      # reinstall git hooks only
```

## What Gets Automated on Every Run
- Git hooks reinstalled (pre-commit, pre-push)
- Safe pull with 3-retry rebase + stash pop
- `nina_index.json` + `nina_index.md` regenerated if any `.py/.md/.json/.sh` changed
- `nina_repo_hygiene_dashboard.md` rewritten (stale docs, large files, open PRs)
- Dead-code scan via `vulture` (weekly, or `audit` mode)
- Duplicate file hash scan (weekly, or `audit` mode)
- All artefacts auto-staged + committed with `--no-verify`
- Push skipped if open PRs exist (Telegram alert sent)
- `nina.service` restarted if `.py` or `.service` files changed
- Crash trap → Telegram alert on any `ERR`

## Contingency Table

| Scenario | Handled by |
|---|---|
| Dirty index from any agent | Auto-stash before pull |
| Stash pop conflict | `checkout --` + `stash drop` |
| Pull fails 3× | Alert + continue with local state |
| Push rejected (remote ahead) | Pull-rebase + retry once |
| Stash pile-up (>20) | `git stash clear` auto |
| `nina.service` unresponsive | `systemctl --user restart` |
| Open PRs during push | Skip push + Telegram alert |
| `update_index.py` not found | Warning only, non-fatal |
| `vulture` not installed | Warning only, skip scan |
| Crash anywhere | ERR trap → Telegram alert |
| PYTHONPATH wrong | Injected via `PY()` helper |
| Hooks missing post-clone | Phase 0 reinstalls always |

## Agents — Who Does What

| Agent | Role | Calls sync? |
|---|---|---|
| **Baizid** | Owner / final decisions | `bash scripts/nina_sync.sh` |
| **Jules** | Code implementation via PRs | Perplexity triggers sync post-merge |
| **agy** | Autonomous task runner | Calls sync after each task |
| **Gemini** | Spec & design | Perplexity triggers sync after writes |
| **Cursor/OpenCode** | Local IDE edits | `bash scripts/nina_sync.sh` before close |
| **Perplexity** | Architect Overwatch | Read-only via GitHub MCP; triggers sync via instruction |
| **cron/systemd** | Scheduled | `OnCalendar=*:0/30` → `nina_sync.sh` |

## Service Management
```bash
systemctl --user status nina.service
systemctl --user restart nina.service
journalctl --user -u nina.service -f
```
NEVER use `sudo systemctl` — always `systemctl --user`.

## Logs
```
logs/nina_sync.log          # per-run OODA trace
logs/dead_code_report.txt   # vulture output (weekly)
logs/duplicate_files.txt    # md5 dup scan (weekly)
```

## ARCHITECTURE.md
# NINA Architecture

> Version: **v14.5** | Updated: 2026-06-22
> Owner: M. Baizid Alam | ASUS VivoBook X530FN · Ubuntu 26.04 · Python 3.14.4

---

## §1 Overview

NINA is a personal AI infrastructure system — a multi-agent orchestration platform running locally at `~/nina` on Ubuntu. She receives commands via Telegram, routes them to the right agent or tool, and manages a persistent async loop.

```
User (Telegram)
     │
     ▼
 interfaces/telegram_bot.py
     │
     ▼
 core/router.py  ──►  NinaGate  ──►  agents/ninamcp/   (MCP tools)
     │                               agents/agy/        (CrewAI tasks)
     │                               agents/jules/      (GitHub coding)
     │                               agents/opencode/   (local coding)
     │                               agents/gemini/     (Gemini CLI)
     │                               agents/perplexity/ (research)
     ▼
 core/ooda.py  (Observe → Orient → Decide → Act loop)
     │
     ▼
 core/memory.py  ──►  data/memory/
```

---

## §2 Directory Structure

| Directory | Role | Ephemeral? |
|---|---|---|
| `agents/` | All external AI agent integrations | No |
| `agents/ninamcp/` | NinaMCP server — MCP protocol bridge | No |
| `agents/opencode/` | OpenCode agent + dispatch tool | No |
| `agents/agy/` | CrewAI-based agy agent | No |
| `agents/jules/` | Jules (Google AI) coding agent | No |
| `agents/gemini/` | Gemini CLI agent | No |
| `agents/perplexity/` | Perplexity research integration | No |
| `core/` | Router, memory, OODA, token guard | No |
| `agent/` | Nina's own agent loop | No |
| `interfaces/` | Telegram, CLI, API | No |
| `ninagate/` | Reverse-proxy gateway service | No |
| `mcp/` | MCP tool definitions | No |
| `config/` | Static config | No |
| `scripts/` | Shell scripts (nina_sync, ensure_env, etc.) | No |
| `bin/` | Binaries (guardian) | No |
| `tools/` | Python dev/ops utilities | No |
| `data/` | Persistent data — registry, memory, graphs | No |
| `runtime/` | Live ephemeral state — **do NOT edit manually** | **Yes** |
| `runtime/state/` | Live JSON state (OODA, Jules, sessions) | Yes |
| `runtime/locks/` | Lock files (.lock) | Yes |
| `runtime/cache/` | Cache files | Yes |
| `runtime/logs/` | Runtime logs, telemetry, gemini scratch | Yes |
| `docs/` | Documentation | No |
| `logs/` | Historical audit logs | No |

---

## §3 Agent Registry

| Agent | Location | Trigger | Model | Gateway |
|---|---|---|---|---|
| **NinaMCP** | `agents/ninamcp/` | MCP calls from OpenCode/Cursor | — | direct |
| **OpenCode** | `agents/opencode/` | `opencode:` Telegram prefix | Claude / GPT | NinaGate |
| **agy** | `agents/agy/` | `agy:` Telegram prefix | Gemini Pro | NinaGate |
| **Jules** | `agents/jules/` | GitHub PR/issue triggers | Gemini 2.5 | GitHub API |
| **Gemini CLI** | `agents/gemini/` | `gemini:` Telegram prefix | Gemini 2.5 | NinaGate |
| **Perplexity** | `agents/perplexity/` | `perplexity:` prefix | sonar-pro | NinaGate |

---

## §4 NinaGate

NinaGate (`ninagate/`) is a FastAPI reverse proxy that brokers all outbound AI calls. It handles:
- API key rotation
- Rate limiting & token budget enforcement
- Session tracking (`runtime/state/ninagate_sessions.json`)
- Provider health monitoring (`runtime/state/provider_health.json`)

NinaGate is a **service**, not an agent — it stays at repo root, not under `agents/`.

```bash
systemctl --user status ninagate
systemctl --user restart ninagate
```

---

## §5 OpenCode Integration

OpenCode (`agents/opencode/`) provides local AI coding capability:

```
agents/opencode/
├── tools/
│   └── opencode_tool.py    # dispatch(task) → runs opencode CLI subprocess
├── tasks/
│   └── TASK_TEMPLATE.md    # task spec format
├── prompts/
│   └── system_prompt.md    # Nina context injected into OpenCode
└── logs/                   # run logs (also mirrored to runtime/logs/opencode/)
```

The `opencode.json` at repo root configures NinaMCP as an MCP provider for OpenCode — giving it full Nina tool access.

Dispatch pattern:
```python
from agents.opencode.tools.opencode_tool import dispatch_async
result = await dispatch_async("implement X in core/router.py")
```

---

## §6 Runtime State

All ephemeral runtime files live under `runtime/`. This directory is **never edited manually** and can be wiped cleanly:

```bash
rm -rf runtime/state/* runtime/locks/* runtime/cache/*
# DO NOT wipe runtime/logs/ — they are audit trail
```

| Subdirectory | Contents |
|---|---|
| `runtime/state/` | ooda_state, jules_watchdog, ninagate_sessions, provider_health, session_* |
| `runtime/locks/` | nina.lock, ninajulesgithub.lock, juleslock.txt |
| `runtime/cache/` | router_cache, model_cache, token_guard_cache |
| `runtime/logs/` | telemetry.jsonl, gemini_scratch.jsonl, opencode run logs |

---

## §7 Data (Persistent)

`data/` holds permanent reference data that survives restarts:

| File | Purpose |
|---|---|
| `data/symbol_map.json` | Codebase symbol index (372KB) |
| `data/dependency_graph.json` | Module dependency graph |
| `data/capabilities.json` | Nina capability registry |
| `data/jules_registry.json` | Jules task history (61KB) |
| `data/healthcheck_registry.txt` | Service health definitions |
| `data/memory/` | Long-term memory store |
| `data/graphs/` | Graph data |
| `data/plans/` | Planning artifacts |
| `data/reports/` | Generated reports |

---

## §8 Key Services

```bash
# Nina main service
systemctl --user status nina
systemctl --user restart nina

# NinaGate proxy
systemctl --user status ninagate
systemctl --user restart ninagate

# NinaMCP server
systemctl --user status ninamcp
systemctl --user restart ninamcp

# Sync + push to GitHub
bash scripts/nina_sync.sh
```

---

## §9 Governance Rules (Non-Negotiable)

1. **Always read live repo via GitHub MCP** before any answer — never assume from memory
2. **Before any fix**: read `docs/nina_error_register.md` + `docs/jules_backlog.md` + target file
3. **Never use `sudo systemctl`** — use `systemctl --user` only
4. **Runtime files** are ephemeral — never commit lock files or cache to main data
5. **All file reads/writes** via GitHub MCP only (from Architect Overwatch context)
6. **Bash scripts** only for: `systemctl`, `nina_sync.sh`, compile checks, log tailing

---

## §10 OODA Loop

Nina operates on a continuous Observe → Orient → Decide → Act loop managed by `core/ooda.py`. State is persisted to `runtime/state/ooda_state.json`.

---

## §11 Memory Architecture

```
Short-term:  runtime/state/session_memory.jsonl   (current session)
Long-term:   data/memory/                          (persisted across restarts)
Shared:      data/crew_shared_memory.json          (multi-agent shared context)
```

---

## §12 Scripts & Binaries

| Path | Purpose |
|---|---|
| `scripts/nina_sync.sh` | Sync, regenerate indices, push to GitHub |
| `scripts/ensure_env.sh` | Verify venv + env vars before service start |
| `scripts/nina_test_mcp.sh` | Test NinaMCP endpoints |
| `bin/guardian` | Guardian binary (process watchdog) |
| `nina_cleanup_sprint.sh` | Root-level cleanup script (pending move to scripts/) |

