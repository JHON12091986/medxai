# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environment
venv/
env/
.venv/

# Nina runtime cache — DO NOT commit (rebuild with: make warm-cache)
.cache/

# Environment / secrets
.env.local
*.local.env

# ── key_resolver persistence layer — never commit ──────────────────
data/secrets.json

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
*.swo
*~

# Test outputs
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# Node (if any)
node_modules/

# Logs (runtime, not telemetry.jsonl which is intentionally tracked)
*.log
nina_runtime*.log

# Generated by post-commit hook — not source-controlled state
nina_context_graph.json
docs/generated/nina_commit_index.md
docs/generated/nina_context.md
docs/generated/bench_report.md
docs/generated/agy_session.md
docs/generated/nina_sh_py_audit.md
docs/generated/nina_md_audit.md
docs/generated/nina_non_sh_py_md_audit.md

# Old dot folder names — superseded by gemini/ agy/ jules/
# Exclude if they still exist locally (e.g. tool-created)
.gemini/
.agy/
.agyspec/
# .jules/ — TRACKED: agent config dir per FOLDER_TREE spec

# Jules runtime files
jules/tasks/*.lock
jules/tasks/*.tmp

# Agy runtime outputs
agy/outputs/
agy/*.tmp
tools/.cursor/
tools/.agy/
tools/AGENTS.md
tools/GEMINI.md
.cache/

# Agent runtime skill dirs (IDE/agent use only)
agy/skills/
.agy/skills/
.cursor/rules/

# ── Runtime / Lock files ──────────────────────────────────────────
data/nina.pid
data/nina.lock
data/pipeline.lock
data/*.lock
jules/data/*.lock
data/nina.lock
data/circuit_state.json
data/ooda_state.json
data/quota_state.json
data/token_guard_cache.json
data/router_cache.json
data/ninagate_cache.json
data/ninagate_sessions.json
data/provider_health.json
data/session_ledger.json
data/jules_watchdog_state.json
data/jules_attempts.json
data/jules_auto_responded.json
data/jules_seen_activities.json
data/jules_registry.json
data/cron_health.json
data/crew_shared_memory.json
data/heartbeats.json
data/healthcheck_registry.txt
data/.guardian_hash
logs/.claude_feed_stamp

# ── Rotating log files ────────────────────────────────────────────
logs/nina.log.*
logs/tools.log.*
logs/upgrade.log.*
logs/nina.*.jsonl
logs/ninagate_stress_*.txt
logs/ninajulesgithub.log.*
logs/archive.tar.gz
logs/event_bus.jsonl
logs/dead_code_report.txt
logs/duplicate_files.txt
logs/intjm_drift.txt
logs/jules_unblock_log.txt
logs/pr_audit_log.txt
logs/registry_orphans.txt
logs/symlink_audit.txt
logs/nina_problem_log.md

# ── Binary / Database blobs ───────────────────────────────────────
data/memory/chromadb/
data/memory/knowledge_base.db
data/test_alpaca_dataset.json

# ── Backup directories (keep archive/ tracked, skip timestamped snaps) ──
upgrades/backups/backup_*/
upgrades/backups/guardian_auto_*/
upgrades/backups/healthcheck_*/
upgrades/backups/core_cleanup/
upgrades/backups/docbase_*/
upgrades/backups/logbase_*/
upgrades/backups/memory*/
upgrades/backups/bugfix_*/
upgrades/backups/nina_codebase_backup_*.md
upgrades/backups/codebase_*.tar.gz
upgrades/backups/memory.py.bak.*

# ── Aider AI tool files ───────────────────────────────────────────
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v4/

# ── Temp / backup file patterns ───────────────────────────────────
*.bak
*.bak.*
*.save
*.save.*
*.selfcheck*
*.fix1.*
*.fix2.*
ninagate/main.py.bak
scripts/nina_sync.sh.bak*
git-hooks/post-commit.bak
bin/nina.bak.*
README.md.bak.*
CHANGELOG.md.bak.*

# ── Env credential backups ────────────────────────────────────────
.env.bak.*
.env.save

# ── Daily runtime proposals (generated by OODA, not source) ──
data/proposals/

# ── Nested log duplicates ──
data/logs/nina_update_log.md
data/logs/

# ── More backup subdirs not yet covered ──
upgrades/backups/root_bak_cleanup/
upgrades/backups/tools_cleanup/
upgrades/backups/bugfix_*/
upgrades/backups/nina_update_log.bak.*

# ── Snapshots committed as .md (extra patterns) ──
upgrades/backups/nina_codebase_backup_*.md
upgrades/backups/nina_context_backup_*.md

# ── Misc runtime state files ──
data/proposals.json
data/jules_auto_responded.json
data/ninatestrunner_report.json
data/nina_update_log.bak.*
logs/nina_update_log.md
logs/nina.jsonl
data/graphs/

# Large backup archives
upgrades/backups/*.tar.gz
upgrades/backups/*.zip
push_in_flight
nina_update_log.md
tools/docs/generated/nina_commit_index.md

# Runtime — live ephemeral state (never commit)
runtime/state/*.json
runtime/cache/*.json
runtime/db/*.db
runtime/logs/*.jsonl
runtime/logs/opencode/
runtime/locks/*.txt

# Runtime — phase 2 additions
runtime/state/*.jsonl
runtime/state/*.md
runtime/locks/*.lock
runtime/cache/*.json
.env
.push_in_flight
