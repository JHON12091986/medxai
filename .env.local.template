# =============================================================================
# NINA .env.local.template — SINGLE SOURCE OF TRUTH
# Copy to .env.local and fill in real values.
# .env.local is NEVER committed to git.
# core/config.py loads .env first, then .env.local (overrides).
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED — Nina will NOT start without these
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
# AUTHORIZED_USER_ID= # DEAD — use TELEGRAM_CHAT_ID instead
TELEGRAM_CHAT_ID=
TELEGRAMCHATID=                  # alias used by load_config (keep in sync)
API_SECRET_KEY=                   # auto-generated if missing, but set it explicitly

# -----------------------------------------------------------------------------
# INFRASTRUCTURE
# -----------------------------------------------------------------------------
OLLAMAHOST=http://localhost:11434

# -----------------------------------------------------------------------------
# AI PROVIDER API KEYS (all optional — disables that provider if missing)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=
GROQ_API_KEY=
CEREBRAS_API_KEY=
MISTRAL_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
PERPLEXITY_API_KEY=
TOGETHER_API_KEY=
COHERE_API_KEY=
FIREWORKS_API_KEY=
XAI_API_KEY=
SAMBANOVA_API_KEY=
HYPERBOLIC_API_KEY=
NOVITA_API_KEY=
ONEBRAINAPIKEY=
ONEBRAINAPIBASE=

# -----------------------------------------------------------------------------
# MODEL OVERRIDES (optional JSON dict mapping provider IDs to model strings)
# -----------------------------------------------------------------------------
MODEL_OVERRIDES={}

# -----------------------------------------------------------------------------
# JULES / GCP
# -----------------------------------------------------------------------------
JULES_API_KEY=
GCP_PROJECT_ID=

# -----------------------------------------------------------------------------
# EWS (Exchange / BASIC Bank email integration)
# -----------------------------------------------------------------------------
EWS_USERNAME=
EWSPASSWORD=
EWS_MY_EMAIL=
EWS_SHARED_EMAIL=

# -----------------------------------------------------------------------------
# IDLE / BEHAVIOUR TUNING
# -----------------------------------------------------------------------------
IDLE_AUTO_APPROVE=false
IDLE_THRESHOLD_MIN=15
IDLE_REPORT_MIN=30

# -----------------------------------------------------------------------------
# RESOURCE GUARDS
# -----------------------------------------------------------------------------
RAM_GUARD_GB=10.5

# -----------------------------------------------------------------------------
# DEAD MAN'S SWITCH (optional — ping URL if Nina goes silent)
# -----------------------------------------------------------------------------
DEADMANPINGURL=

# -----------------------------------------------------------------------------
# HYPERDRIVE (parallel fan-out router)
# -----------------------------------------------------------------------------
HYPERDRIVE_ENABLED=true
