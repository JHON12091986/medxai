# Dirty File Register

These files are **always regenerated by NINA processes** and will appear as modified in `git status`. This is **expected and not a bug**.

Do not manually edit, stage, or commit these files. They are handled automatically.

| File | Written By | Gitignored? | Auto-committed? |
|---|---|---|---|
| `data/router/provider_metrics.db` | Router service (live) | ✅ Yes | N/A |
| `nina_context_graph.json` | `post-commit` hook | ✅ Yes | N/A |
| `docs/generated/nina_commit_index.md` | `post-commit` hook | ✅ Yes | N/A |
| `docs/generated/nina_context.md` | NINA autonomy | ✅ Yes | N/A |
| `docs/generated/bench_report.md` | Bench runner | ✅ Yes | N/A |
| `docs/space/nina_repo_hygiene_dashboard.md` | `pre-push` hook | ❌ Tracked | ✅ Yes (post-commit auto-commits) |
| `docs/space/nina_index.md` | `update_index.py` | ❌ Tracked | ✅ Yes (post-commit auto-commits) |
| `docs/context/nina_session_log.md` | NINA session logger | ❌ Tracked | ✅ Yes (post-commit auto-commits) |

## If You See These Files Dirty

```bash
# Quick reset — discard all known dirty generated files
git checkout -- \
  docs/space/nina_index.md \
  docs/space/nina_repo_hygiene_dashboard.md \
  docs/context/nina_session_log.md 2>/dev/null || true
```

`provider_metrics.db` and `nina_context_graph.json` are gitignored so they will never appear in `git status`.
